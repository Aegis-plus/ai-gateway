{
  "name": "ai-gateway",
  "version": "0.1.0",
  "description": "Lightweight multi-account gateway for Kiro and Google Antigravity with an OpenAI- and Anthropic-compatible API",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun --watch src/index.ts",
    "dev:watch": "bun --watch src/index.ts",
    "start": "bun src/index.ts",
    "fetch-models": "bun src/fetch-models.ts",
    "typecheck": "tsc --noEmit",
    "test": "bun test"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/node": "^22.10.0",
    "typescript": "^5.7.0"
  }
}
